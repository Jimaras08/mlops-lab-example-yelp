name: Yelp Dataset Classification

docker_env:
    image: yelp_dataset:v1.0

entry_points:
  # TODO: env PYTHONPATH=/appl
  main:
    parameters:
      data_file: dataset_split.pkl
    command: "python3 train_model_lstm.py"
